{
  "name": "portfolio-site",
  "version": "1.0.0",
  "scripts": {
    "build:css": "npx postcss src/css/styles.css -o docs/styles.css",
    "build": "npm run build:css && npx eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "tailwindcss": "^3.3.2",
    "postcss": "^8.4.21",
    "autoprefixer": "^10.4.14"
    "postcss-cli": "^10.1.0"
  }
}

